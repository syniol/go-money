name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: [1.23.x, 1.24.x]
    
    steps:
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: ${{ matrix.go-version }}

    - name: Check out code
      uses: actions/checkout@v4

    - name: Cache Go modules
      uses: actions/cache@v3
      with:
        path: ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Download dependencies
      run: go mod download

    - name: Run tests
      run: go test -v -race -coverprofile=coverage.out ./...

    - name: Enforce coverage floor
      run: |
        # Exclude the code generator (build tool, not runtime code) from the
        # floor so its 0% coverage does not drag the library number down.
        # The first line is the "mode:" header; keep it verbatim.
        awk 'NR == 1 || $0 !~ /^github\.com\/syniol\/go-money\/cmd\//' coverage.out > coverage.trimmed.out
        pct=$(go tool cover -func=coverage.trimmed.out | awk '/^total:/ {print $3}' | tr -d '%')
        awk -v p="$pct" 'BEGIN { if (p+0 < 85.0) { printf "coverage %.1f%% is below 85%% floor\n", p; exit 1 } else { printf "coverage %.1f%% meets 85%% floor\n", p } }'

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.out
        flags: unittests
        name: codecov-umbrella

  vet:
    name: Vet
    runs-on: ubuntu-latest

    steps:
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: 1.24.x

    - name: Check out code
      uses: actions/checkout@v4

    - name: Cache Go modules
      uses: actions/cache@v3
      with:
        path: ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Download dependencies
      run: go mod download

    - name: Run go vet
      run: go vet ./...

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.24.x

      - name: Check out code
        uses: actions/checkout@v4

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v4
        with:
          version: latest
          args: --timeout=5m --out-format=colored-line-number

  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: 1.24.x

    - name: Check out code
      uses: actions/checkout@v4

    - name: Cache Go modules
      uses: actions/cache@v3
      with:
        path: ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Download dependencies
      run: go mod download

    - name: Build
      run: go build -v ./...

  staticcheck:
    name: Staticcheck
    runs-on: ubuntu-latest

    steps:
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: 1.24.x

    - name: Check out code
      uses: actions/checkout@v4

    - name: Cache Go modules and installed tools
      uses: actions/cache@v3
      with:
        path: |
          ~/go/pkg/mod
          ~/go/bin
        key: ${{ runner.os }}-staticcheck-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-staticcheck-

    - name: Install staticcheck
      run: |
        if [ ! -x "$HOME/go/bin/staticcheck" ]; then
          go install honnef.co/go/tools/cmd/staticcheck@latest
        fi

    - name: Run staticcheck
      run: $HOME/go/bin/staticcheck ./...

  govulncheck:
    name: Govulncheck
    runs-on: ubuntu-latest

    steps:
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: 1.24.x

    - name: Check out code
      uses: actions/checkout@v4

    - name: Cache Go modules and installed tools
      uses: actions/cache@v3
      with:
        path: |
          ~/go/pkg/mod
          ~/go/bin
        key: ${{ runner.os }}-govulncheck-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-govulncheck-

    - name: Install govulncheck
      run: |
        if [ ! -x "$HOME/go/bin/govulncheck" ]; then
          go install golang.org/x/vuln/cmd/govulncheck@latest
        fi

    - name: Run govulncheck
      run: $HOME/go/bin/govulncheck ./...

  generate:
    name: Generate
    runs-on: ubuntu-latest

    steps:
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: 1.24.x

    - name: Check out code
      uses: actions/checkout@v4

    - name: Cache Go modules
      uses: actions/cache@v3
      with:
        path: ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Download dependencies
      run: go mod download

    - name: Run go generate
      run: go generate ./...

    - name: Verify no drift
      run: |
        if ! git diff --exit-code; then
          echo "::error::Generated files are out of date. Run 'go generate ./...' and commit the result."
          exit 1
        fi